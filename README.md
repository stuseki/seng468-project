# seng468-project
A semantic retrieval system that allows users to signup and login, as well as upload, search, and delete PDFs.

## Executing
* Build the project with `docker compose up`
* View/interact with the UI at `http://localhost:8080/` in your browser.


